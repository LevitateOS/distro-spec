[package]
name = "distro-spec"
version = "0.1.0"
edition = "2021"
description = "Installation specifications for LevitateOS, AcornOS, IuppiterOS, and RalphOS"
license = "MIT OR Apache-2.0"

[features]
default = ["std"]
std = []

[dependencies]
libc = "0.2"
smallvec = "1.11"
distro-contract = { path = "../distro-contract" }
