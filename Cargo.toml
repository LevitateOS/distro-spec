[package]
name = "distro-spec"
version = "0.1.0"
edition = "2021"
description = "Installation specifications for LevitateOS and AcornOS"
license = "MIT OR Apache-2.0"

[features]
default = ["std"]
std = []

[dependencies]
